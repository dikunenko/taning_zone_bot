# Основная библиотека для Telegram бота
python-telegram-bot==13.15
requests==2.31.0
python-dotenv==1.0.1

# Для HTTP запросов к API DeepSeek и YandexGPT
requests==2.31.0

# Для работы с JSON (обычно уже есть, но на всякий случай)
ujson==5.9.0

# Для работы с переменными окружения
python-dotenv==1.0.1

# Для работы с датами и временем (улучшенная версия)
python-dateutil==2.8.2

# Для логирования (полезно для отладки на хостинге)
loguru==0.7.2

# Для повторных попыток при сбоях API
tenacity==8.2.3

# Для кэширования (чтобы не дергать API при каждом запросе)
cachetools==5.3.2

# Для асинхронной работы (улучшает производительность)
aiohttp==3.9.3

# Для работы с файлами (сохранение контекста)
pathlib2==2.3.7

# Для обработки ошибок и ретраев
backoff==2.2.1
