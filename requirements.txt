# Основная библиотека для Telegram бота (стабильная версия 13.15)
python-telegram-bot==13.15

# HTTP запросы к API DeepSeek и YandexGPT
requests==2.31.0

# Работа с переменными окружения
python-dotenv==1.0.0

# Работа с SQLite3 (встроен в Python, но для явности указываем)
# sqlite3 - уже есть в Python, отдельно устанавливать не нужно

# Для работы с датами (улучшенная версия)
python-dateutil==2.8.2

# Для повторных попыток при сбоях API
tenacity==8.2.3

# Для логирования (полезно для отладки на хостинге)
loguru==0.7.2

# Для обработки ошибок и ретраев
backoff==2.2.1

# Для работы с JSON (более быстрая версия)
ujson==5.9.0

# Для работы с временными зонами
pytz==2024.1
